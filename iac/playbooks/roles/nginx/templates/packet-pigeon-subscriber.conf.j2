server {
    listen [::]:{{ http_port }} ipv6only=on;
    listen {{ http_port }};

    server_name {{ http_host }};

    location / {
        proxy_pass http://127.0.0.1:{{ upstream_port }}$request_uri;
    }
}
