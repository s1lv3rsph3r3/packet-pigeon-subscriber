module.exports = (() => ({
  configuration: 'Put all configuration requirements in this file',
  credentials:
    'Credentials for the module plugin should be kept within the .env file - only an example should be committed to repository',
}))();
